load("@io_bazel_rules_go//go:def.bzl", "go_test")

go_test(
    name = "arloIssue29_test",
    srcs = ["arloIssue29.go"],
    data = [
        "//tools/test:database_config.json",
        "//cvl/conf:cvl_cfg.json",
    ],
    # Set DB_CONFIG_PATH to point to the config file in runfiles
    env = {
        "DB_CONFIG_PATH": "tools/test/database_config.json",
    },
    # Run from the runfiles root so relative paths work
    rundir = ".",
    visibility = ["//visibility:public"],
    deps = [
        "//translib/db",
        "//translib/tlerr",
        "@com_github_golang_glog//:glog",
    ],
)

go_test(
    name = "testdb_test",
    srcs = ["testdb.go"],
    data = [
        "//tools/test:database_config.json",
        "//cvl/conf:cvl_cfg.json",
    ],
    # Set DB_CONFIG_PATH to point to the config file in runfiles
    env = {
        "DB_CONFIG_PATH": "tools/test/database_config.json",
    },
    # Run from the runfiles root so relative paths work
    rundir = ".",
    visibility = ["//visibility:public"],
    deps = [
        "//translib/db",
        "//translib/tlerr",
        "@com_github_golang_glog//:glog",
    ],
)

go_test(
    name = "testmap_test",
    srcs = ["testmap.go"],
    data = [
        "//tools/test:database_config.json",
        "//cvl/conf:cvl_cfg.json",
    ],
    # Set DB_CONFIG_PATH to point to the config file in runfiles
    env = {
        "DB_CONFIG_PATH": "tools/test/database_config.json",
    },
    # Run from the runfiles root so relative paths work
    rundir = ".",
    visibility = ["//visibility:public"],
    deps = [
        "//translib/db",
        "//translib/tlerr",
        "@com_github_golang_glog//:glog",
    ],
)

go_test(
    name = "testsubscribe_test",
    srcs = ["testsubscribe.go"],
    data = [
        "//tools/test:database_config.json",
        "//cvl/conf:cvl_cfg.json",
    ],
    # Set DB_CONFIG_PATH to point to the config file in runfiles
    env = {
        "DB_CONFIG_PATH": "tools/test/database_config.json",
    },
    # Run from the runfiles root so relative paths work
    rundir = ".",
    visibility = ["//visibility:public"],
    deps = [
        "//translib/db",
        "//translib/tlerr",
        "@com_github_golang_glog//:glog",
    ],
)
