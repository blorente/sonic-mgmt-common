# NOTE: These test files use legacy CVL APIs that have been refactored.
# The tests require significant updates to work with the current API:
# - CVLEditConfigData, VALIDATE_*, OP_* types moved to cvl/common package
# - ValidationSessOpen() now requires a common.DBAccess parameter
#
# These tests are disabled until they can be updated.

# Uncomment and update when migrating to new API:
#
# load("@io_bazel_rules_go//go:def.bzl", "go_binary", "go_library", "go_test")
#
# go_test(
#     name = "cfg_validator_test",
#     srcs = ["cfg_validator.go"],
#     ...
# )
